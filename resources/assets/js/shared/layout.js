$(document).ready(function() {
    /*$('.sticky').each(function(ind, elem) {
        new Foundation.Sticky($(elem), {});
    });
    $('body').on('stuckto.zf.sticky', '[data-sticky]', function(e) {
        console.log('sticky');
        $(elem).addClass('is-stuck');
    });
    $('body').on('unstuckfrom.zf.sticky', '[data-sticky]', function(e) {
        console.log('unsticky');
        $(elem).removeClass('is-stuck');
    });*/
});